---
phase: 05-orchestrator
plan: 03
type: execute
---

<objective>
Build Task Center UI: run history, live DAG, filtered logs, artifact preview, retry failed node, and manual skip.

Purpose: Make multi-agent runs observable and recoverable (SPEC §5.5, §10).
Output: Tasks view fully live; Phase 5 complete after verify.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/05-orchestrator/05-02-SUMMARY.md
@src/ui/app-shell.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Task history + DAG + log stream UI</name>
  <files>src/ui/tasks/center.ts, src/ui/tasks/dag.ts, src/ui/tasks/logs.ts, src/main.ts</files>
  <action>
    Replace mock task list with `list_task_runs`. Selecting a run loads nodes + logs.

    Render DAG from nodes/depends_on (SVG or simple HTML graph — keep prototype look).

    Subscribe to `task-log` / `task-run-updated` events while run active. Filter tabs by agent.

    Artifact panel: read first artifact path via `read_workspace_file` command (path constrained to agent workspace); show text preview + copy.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Task center displays live runs</done>
</task>

<task type="auto">
  <name>Task 2: Retry and skip commands + UI buttons</name>
  <files>src-tauri/src/services/dag_runner.rs, src/ui/tasks/center.ts</files>
  <action>
    `retry_node(run_id, node_id)`: only if failed; increment retry_count; set pending; ensure runner continues/resumes.

    `skip_node(run_id, node_id)`: set skipped; do not auto-skip dependents; runner continues other ready nodes.

    Wire prototype buttons「失败重试」「人工跳过」.
  </action>
  <verify>`cargo check` + `npm run build`</verify>
  <done>Intervention actions work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Orchestrate → Dispatch → Task Center loop.</what-built>
  <how-to-verify>
    1. Orchestrate a multi-agent goal (or fixture plan if CLI flaky)
    2. Dispatch; watch DAG/logs update
    3. Force a failure if possible → retry or skip
    4. Confirm history survives restart
  </how-to-verify>
  <resume-signal>Type "approved" — Phase 5 complete</resume-signal>
</task>

</tasks>

<verification>
- [ ] End-to-end commander→tasks path works
- [ ] Human approved
</verification>

<success_criteria>
- Phase 5 Orchestrator complete
</success_criteria>

<output>
Create `.planning/phases/05-orchestrator/05-03-SUMMARY.md` — Phase 5 complete.
</output>
