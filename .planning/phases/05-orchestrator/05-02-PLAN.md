---
phase: 05-orchestrator
plan: 02
type: execute
---

<objective>
Implement Dispatch: create TaskRun + DAG nodes from a Plan, then execute ready nodes via EngineAdapter respecting depends_on (max 1–2 concurrent).

Purpose: Multi-agent execution is the core product loop (SPEC §5.4–5.5).
Output: Runner updates node/run status and streams logs to DB + events.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/05-orchestrator/05-01-SUMMARY.md
@src-tauri/src/engines/runner.rs
@src-tauri/src/repo/tasks.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch creates run graph from plan</name>
  <files>src-tauri/src/services/dispatch.rs, src-tauri/src/commands/tasks.rs</files>
  <action>
    `dispatch_plan(plan_id)`: load plan JSON, create task_run + task_nodes (map depends_on ids), status pending, return run_id.

    Idempotency: refuse if plan already dispatched (or allow re-dispatch as new run — prefer new run each time).
  </action>
  <verify>`cargo test` dispatch fixture plan → N nodes</verify>
  <done>DB contains runnable DAG</done>
</task>

<task type="auto">
  <name>Task 2: DAG runner loop with log persistence + events</name>
  <files>src-tauri/src/services/dag_runner.rs, src-tauri/src/commands/tasks.rs, src/lib/api/tasks.ts</files>
  <action>
    `start_run(run_id)`: loop while pending/running nodes exist.

    Ready = all depends_on in success (skipped counts as success-for-deps? **No** — skipped blocks dependents unless policy says otherwise; SPEC: skipped allows continuing other ready nodes; dependents of skipped stay blocked unless also skipped — implement: dependency satisfied only if predecessor status == success).

    Concurrency: semaphore 1 (default) or 2.

    For each node: build prompt from plan subtask; cwd = agent workspace; engine/model/reasoning from node; stream logs → task_logs + `task-log` events; set artifact_paths if CLI prints a known marker or subtask declares paths.

    Update run.progress = finished/total. Terminal states: all success → run success; any failed with none running → run failed.

    `cancel_run` best-effort kill children.
  </action>
  <verify>`cargo check` passes; add ignored e2e note</verify>
  <done>Runner executes dependency-aware nodes</done>
</task>

</tasks>

<verification>
- [ ] Dispatch + runner compile and unit-tested where possible
</verification>

<success_criteria>
- Backend DAG execution works
</success_criteria>

<output>
Create `.planning/phases/05-orchestrator/05-02-SUMMARY.md`
</output>
