---
phase: 05-orchestrator
plan: 01
type: execute
---

<objective>
Add Orchestrator settings UI (independent CLI/model/reasoning) and implement Orchestrate flow that calls the configured engine to produce validated Plan JSON for the commander workbench.

Purpose: Separate brain config from worker agents (product decision #2).
Output: Settings persist; Orchestrate renders real/fallback plan matrix.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/04-cli-runtime/04-03-SUMMARY.md
@src/lib/api/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrator settings UI</name>
  <files>src/ui/orchestrator/settings.ts, src/ui/app-shell.html, src/main.ts</files>
  <action>
    Add a compact settings panel on 调度中枢 (or settings section in sidebar footer): CLI engine, model, reasoning. Load/save via existing orchestrator settings IPC.

    Defaults from DB seed.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Orchestrator config editable and persisted</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate service — prompt, parse, validate Plan JSON</name>
  <files>src-tauri/src/services/orchestrate.rs, src-tauri/src/commands/orchestrate.rs, src/ui/orchestrator/workbench.ts, src/lib/api/orchestrate.ts</files>
  <action>
    Build prompt including: user goal + catalog of agents (name, description, skills enabled, cli, model). Demand JSON matching SPEC §7.4.

    Run via EngineAdapter using orchestrator settings. Parse JSON (strip markdown fences if needed). Validate agents/skills exist & enabled; normalize/drop invalid skills with warnings.

    Persist goal + plan rows. Return plan to UI.

    On parse failure: return raw output + error; do not create dispatchable plan.

    UI: replace mock `startOrchestration` with real call; render intent / subtasks / routing table from response.
  </action>
  <verify>`cargo check` + `npm run build`; include a unit test for JSON validation helper with fixture</verify>
  <done>Orchestrate produces validated plan structure in UI</done>
</task>

</tasks>

<verification>
- [ ] Settings round-trip
- [ ] Plan validation rejects unknown agents
</verification>

<success_criteria>
- Ready for Dispatch/DAG runner
</success_criteria>

<output>
Create `.planning/phases/05-orchestrator/05-01-SUMMARY.md`
</output>
