---
phase: 03-agent-skill
plan: 02
type: execute
---

<objective>
Scan `.agent/skills/**/*.md` per Agent workspace, sync to DB, and drive Skill list UI with enable toggles and preview.

Purpose: Skills are the unit Orchestrator will route to (SPEC §5.2).
Output: Sync button + skill rows backed by SQLite.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/03-agent-skill/03-01-SUMMARY.md
@src-tauri/src/repo/skills.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skill scanner service</name>
  <files>src-tauri/src/services/skill_scan.rs, src-tauri/src/commands/skills.rs</files>
  <action>
    Walk `{workspace}/.agent/skills/` recursively for `*.md`.

    Parse: filename as relative_path; title = stem; description = first non-empty paragraph or YAML frontmatter `description` if present; content_hash = sha256 of file bytes.

    Sync strategy: upsert by (agent_id, relative_path); remove DB skills whose files disappeared; preserve `enabled` on upsert when hash unchanged / when file still exists.

    Command `sync_agent_skills(agent_id) -> { added, updated, removed }`.
    Command `read_skill_content(agent_id, relative_path) -> string` for preview (path must stay under workspace).
  </action>
  <verify>`cargo test` with temp workspace containing 2 md files</verify>
  <done>Scanner syncs skills correctly</done>
</task>

<task type="auto">
  <name>Task 2: Agent detail Skill list UI</name>
  <files>src/ui/agents/detail-skills.ts, src/ui/modals.ts, src/main.ts</files>
  <action>
    When opening agent detail: load skills; render rows; toggle → `set_skill_enabled`; preview → `read_skill_content` into skill modal body.

    「同步 Workspace Skill」button → `sync_agent_skills` → refresh + toast counts.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Skill UI fully backed by DB/FS</done>
</task>

</tasks>

<verification>
- [ ] Sync detects add/remove
- [ ] Enable persists across restart
</verification>

<success_criteria>
- Skills ready for orchestrator routing
</success_criteria>

<output>
Create `.planning/phases/03-agent-skill/03-02-SUMMARY.md`
</output>
