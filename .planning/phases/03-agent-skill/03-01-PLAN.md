---
phase: 03-agent-skill
plan: 01
type: execute
---

<objective>
Wire Agent import (local path + git clone) and Agent matrix UI to real SQLite data with search/filters.

Purpose: Replace prototype mock cards with registered workspaces.
Output: Import modal persists agents; matrix renders from `list_agents`.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/02-persistence/02-03-SUMMARY.md
@src/ui/app-shell.html
@src/lib/api/agents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend import — validate path / clone git URL</name>
  <files>src-tauri/src/services/import_agent.rs, src-tauri/src/commands/agents.rs</files>
  <action>
    Command `import_agent { name, workspace_path_or_git, default_cli, description? }`:

    - If path exists and is directory → bind it
    - If looks like git URL → `git clone` into `~/Library/Application Support/AgentFlow/workspaces/{name}` (or user-chosen parent later); fail clearly if git missing/clone fails
    - Upsert agent row + default empty profile
    - Do not scan skills yet (03-02)

    Security: reject path traversal tricks; only absolute paths under user home or explicitly selected paths.
  </action>
  <verify>`cargo test` with tempfile dir import; git URL test optional/mockable</verify>
  <done>import_agent creates DB row pointing at real workspace</done>
</task>

<task type="auto">
  <name>Task 2: Render Agent matrix from IPC + wire import modal</name>
  <files>src/ui/agents/matrix.ts, src/ui/agents/import-modal.ts, src/main.ts, src/ui/app-shell.html</files>
  <action>
    On showView('agents') / app load: `list_agents` → render cards (reuse prototype card markup as template). Empty state when none.

    Keep filterAgents client-side on rendered cards.

    Import modal submit → `import_agent` → refresh grid → toast.

    Remove hardcoded six demo cards from shell OR replace at runtime (prefer clear `#agent-grid` then render).
  </action>
  <verify>`npm run build` passes</verify>
  <done>Matrix shows DB agents; import works for local path</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Agent import + live matrix.</what-built>
  <how-to-verify>
    1. Import a local folder as an Agent
    2. See it on Agent 矩阵; filters work
    3. Relaunch app — agent still there
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Local import + list + restart OK
- [ ] Human approved
</verification>

<success_criteria>
- Agent registry usable from UI
</success_criteria>

<output>
Create `.planning/phases/03-agent-skill/03-01-SUMMARY.md`
</output>
