---
phase: 03-agent-skill
plan: 03
type: execute
---

<objective>
Persist Agent detail model/CLI configuration form (engine, model, reasoning, temperature, auto-route, engine_options JSON) and load it when selecting an agent.

Purpose: Phase 4 sandbox and Phase 5 routing need real profiles.
Output: Save/load profile from UI; Phase 3 complete.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/03-agent-skill/03-02-SUMMARY.md
@src/lib/api/agents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind detail form to profile IPC</name>
  <files>src/ui/agents/detail-config.ts, src/ui/app-shell.html, src/main.ts</files>
  <action>
    On selectAgent/load detail: populate CLI select, model, reasoning pills, temperature, auto-route switch, playwright mode → `engine_options_json`.

    「保存 Agent 配置」→ `upsert_agent_profile` + optional agent description/default_cli update.

    Give form controls stable `id`/`data-field` attributes if missing.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Form round-trips through SQLite</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Agent detail config + skills from Phase 3.</what-built>
  <how-to-verify>
    1. Change model/reasoning/CLI, save, relaunch — values restored
    2. Sync skills on a workspace with `.agent/skills`, toggle one off, relaunch — still off
  </how-to-verify>
  <resume-signal>Type "approved" — Phase 3 complete</resume-signal>
</task>

</tasks>

<verification>
- [ ] Profile persists
- [ ] Human approved
</verification>

<success_criteria>
- Phase 3 Agent & Skill complete
</success_criteria>

<output>
Create `.planning/phases/03-agent-skill/03-03-SUMMARY.md` — Phase 3 complete.
</output>
