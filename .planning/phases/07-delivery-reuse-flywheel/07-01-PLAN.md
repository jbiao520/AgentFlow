# 07-01: S1 调度中枢可执行摘要

## Objective

Orchestrate 出 Plan 后，在主按钮上方展示「即将执行」摘要（子任务数、Agent 数、引擎/模型、跨仓库、并发、风险），无需二次弹窗。

## Context

- Design: `docs/plans/2026-08-02-delivery-and-reuse-flywheel-design.md` Part A2
- `src/ui/orchestrator/workbench.ts` — `renderPlanWorkbench`
- `src/ui/concurrency.ts` — concurrency control
- Agent list: `src/lib/api/agents.ts` `listAgents`（workspace 路径判断跨仓库）

## Tasks

### Task 1: 纯函数聚合摘要

- File: `src/ui/orchestrator/exec-summary.ts` (new)
- Export `buildExecSummary(plan, agents?, concurrency): { lines, risks }`
- Fields: N subtasks, M unique agents, engines, models (max 3 +「等」), crossWorkspace, highReasoning count, max dependency depth (approx via depends_on chain)
- Unit-testable pure logic; agents optional (missing → crossWorkspace unknown/false)

### Task 2: 渲染到 workbench

- File: `src/ui/orchestrator/workbench.ts`, `src/styles.css`
- Insert `#orch-exec-summary` card above action row (or between header and timeline); also show before clarify「提交并执行」
- On render, optionally `listAgents()` then re-render summary with cross-workspace
- Concurrency from select: summary shows suggested; live concurrency already in action row

## Verify

- `npm run build` passes
- Manual: Orchestrate → see 即将执行 with N/M/engines; Dispatch still works

## Done

- [ ] exec-summary module
- [ ] UI card in both clarify and non-clarify flows
- [ ] CSS
