# 07-04: S4 模版向导成功出口

## Objective

保存模版成功后不立刻跳模版库；展示成功步：设为定时 / 执行一次 / 查看模版库。

## Context

- Design Part B3
- `src/ui/templates/save-wizard.ts` `commitSave`

## Tasks

### Task 1: 成功步 UI

- After createTemplate, keep modal open; `renderSuccess(template)`
- Buttons: 设为定时 → close + openCreateScheduleModal({templateId, values from defaults})
- 执行一次 → close + showView templates / trigger instantiate run (reuse library run if exported)
- 查看模版库 → close + showView('templates') + refresh

### Task 2: createTemplate 返回值

- Ensure createTemplate returns Template with id; use it for success actions

## Verify

- Save wizard ends on success step with 3 exits
- `npm run build`

## Done

- [ ] Success step
- [ ] Three exit actions
