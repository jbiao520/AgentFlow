# 07-06: S6 总览自动化动态

## Objective

总览增加三块：最近交付、即将运行、可复用模版；点击跳转实体。

## Context

- Design Part A3
- Frontend can compose: listTaskRuns / listSchedules / listTemplates (prefer no new IPC if possible)
- Check existing list runs API

## Tasks

### Task 1: Data load

- Use existing IPC: list runs (terminal, limit 5), listSchedules (enabled, sort next_run_at), listTemplates (sort updated_at)
- If no list-all-runs with status filter, use overview queue + history from tasks API

### Task 2: UI panel

- Insert `#overview-automation-pulse` between stats and recent agents
- Three columns/cards with empty states
- Click: openTaskRun / select schedule view / templates view

## Verify

- Overview shows three sections after load
- `npm run build`

## Done

- [ ] Automation pulse UI
- [ ] Navigation wired
