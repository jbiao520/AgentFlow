# 07-07: S7 定时连续失败暂停

## Objective

记录 `consecutive_failures`；失败累加、成功清零；≥3 自动 `enabled=0` 并写 last_error；总览展示已暂停角标。

## Context

- Design Part B5
- `record_schedule_run_result`, `mark_schedule_fired` in repo/schedules.rs
- migrate SCHEMA_VERSION 8 → 9

## Tasks

### Task 1: Migration + field

- v9: `schedules.consecutive_failures INTEGER NOT NULL DEFAULT 0`
- schema.sql + Schedule struct + SELECT_COLS + TS type

### Task 2: Update failure/success paths

- mark_schedule_fired: on error bump consecutive; if >=3 disable + append「已自动暂停」
- record_schedule_run_result: on failed (no more retries) bump; on success zero; if >=3 disable
- Constant AUTO_PAUSE_AFTER = 3

### Task 3: UI

- Schedule list: show error/paused badge when consecutive_failures high or last_error
- Overview automation: count disabled with last_error containing 暂停 or consecutive >=3

## Verify

- cargo test schedules if any; cargo check
- `npm run build`

## Done

- [ ] Migration v9
- [ ] Auto-pause logic
- [ ] UI badges
