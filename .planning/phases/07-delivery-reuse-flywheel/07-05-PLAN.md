# 07-05: S5 定时快路径创建

## Objective

导出 `openCreateScheduleModal({ templateId, values?, name? })`，不进入 schedules 页即可创建 once/interval 定时。

## Context

- Design Part B4
- Reuse `createSchedule` API and form patterns from `schedules/page.ts`

## Tasks

### Task 1: Modal shell + module

- HTML: `#schedule-create-modal` in app-shell (or dynamic)
- File: `src/ui/schedules/create-modal.ts`
- Fields: name, template (readonly label), variables, mode once|interval, next_run_at / interval, enabled default on
- Submit createSchedule → toast → optional showView schedules

### Task 2: Wire callers

- Export openCreateScheduleModal; init in main.ts
- Call from reuse bar (S3) and save wizard success (S4)

## Verify

- Open from wizard success; create works
- `npm run build`

## Done

- [ ] create-modal.ts
- [ ] Wired from S3/S4
