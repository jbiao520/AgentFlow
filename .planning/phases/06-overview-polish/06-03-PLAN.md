---
phase: 06-overview-polish
plan: 03
type: execute
---

<objective>
Produce a macOS `.app` via `tauri build` and close v1.0 against SPEC §10 acceptance checklist.

Purpose: Ship the personal AgentMind binary.
Output: Built app + acceptance notes; v1.0 milestone ready.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/BRIEF.md
@.planning/phases/06-overview-polish/06-02-SUMMARY.md
@src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build configuration + tauri build</name>
  <files>src-tauri/tauri.conf.json, README.md, src-tauri/capabilities/default.json</files>
  <action>
    Ensure productName AgentMind, icons OK, bundle macOS app. Review capabilities — only required permissions (fs scope for workspaces/app data, shell for CLIs as designed).

    Run `npm run tauri build`. Document artifact path in README (`src-tauri/target/release/bundle/macos/AgentMind.app`).

    Fix any release-only compile errors.
  </action>
  <verify>AgentMind.app exists and launches (`open .../AgentMind.app`)</verify>
  <done>Release bundle builds</done>
</task>

<task type="auto">
  <name>Task 2: Write acceptance record against SPEC §10</name>
  <files>.planning/phases/06-overview-polish/ACCEPTANCE.md, .planning/ROADMAP.md, .planning/MILESTONES.md</files>
  <action>
    Checklist each SPEC §10 item with Pass/Fail/Skip+reason (e.g. second CLI not installed).

    Create `.planning/MILESTONES.md` entry for v1.0 with date and summary.

    Mark Phase 6 and all roadmap phases complete when checklist acceptable.
  </action>
  <verify>ACCEPTANCE.md exists with all five gates addressed</verify>
  <done>v1.0 acceptance documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>macOS AgentMind.app + acceptance checklist.</what-built>
  <how-to-verify>
    1. Launch the built `.app` (not only `tauri dev`)
    2. Smoke: import agent, orchestrate/dispatch or sandbox, overview numbers, Cmd+K
    3. Confirm ACCEPTANCE.md matches what you observed
  </how-to-verify>
  <resume-signal>Type "approved" to mark v1.0 foundation complete</resume-signal>
</task>

</tasks>

<verification>
- [ ] `.app` launches
- [ ] SPEC §10 recorded
- [ ] Human approved
</verification>

<success_criteria>
- Phase 6 complete; AgentMind v1.0 shippable for personal use
</success_criteria>

<output>
Create `.planning/phases/06-overview-polish/06-03-SUMMARY.md` — Phase 6 complete / v1.0.
</output>
