---
phase: 06-overview-polish
plan: 01
type: execute
---

<objective>
Replace overview mock metrics and topology with aggregations from SQLite + active run state.

Purpose: Global view must reflect the real system (SPEC §5.6).
Output: Live stats bar + topology driven by agents and recent/current runs.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/05-orchestrator/05-03-SUMMARY.md
@src/ui/app-shell.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overview aggregation API</name>
  <files>src-tauri/src/commands/overview.rs, src/lib/api/overview.ts</files>
  <action>
    `get_overview_stats()` → agent_count, agents_healthy_pct (idle/working without error), running_tasks, completed_today, success_rate_today, optional token stub (0 or "n/a" if not tracked).

    `get_overview_topology()` → nodes (orchestrator + agents) + edges from current/recent run collaborations.

    `list_running_queue()` for overview table.
  </action>
  <verify>`cargo check` + simple test for counts with fixture DB</verify>
  <done>Overview APIs return real numbers</done>
</task>

<task type="auto">
  <name>Task 2: Bind overview UI</name>
  <files>src/ui/overview/page.ts, src/main.ts</files>
  <action>
    On showView('overview') refresh stats, queue table, and SVG/HTML topology. Clicking agent node → selectAgent; hub → commander.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Overview no longer hardcoded demo metrics</done>
</task>

</tasks>

<verification>
- [ ] Stats change when agents/runs change
</verification>

<success_criteria>
- Overview is truthful
</success_criteria>

<output>
Create `.planning/phases/06-overview-polish/06-01-SUMMARY.md`
</output>
