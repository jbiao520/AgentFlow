---
phase: 06-overview-polish
plan: 02
type: execute
---

<objective>
Polish Cmd+K actions, empty/error states, and permission/path error messaging across the app.

Purpose: Make v1 feel finished for daily personal use.
Output: Robust UX for zero-data and failure paths.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/06-overview-polish/06-01-SUMMARY.md
@src/ui/modals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Cmd+K actions</name>
  <files>src/ui/modals.ts, src/ui/cmdk.ts</files>
  <action>
    Cmd+K actions: jump views; "Probe CLIs"; "New import"; list agents by name filter → open detail; "Open commander".

    Keep keyboard UX snappy; no new design system.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Cmd+K covers primary power-user flows</done>
</task>

<task type="auto">
  <name>Task 2: Empty and error states</name>
  <files>src/ui/agents/matrix.ts, src/ui/tasks/center.ts, src/ui/orchestrator/workbench.ts, src/ui/toast.ts</files>
  <action>
    Empty: no agents / no runs / no skills — clear CTAs.

    Errors: import/clone/CLI missing/orchestrate parse failures show actionable toast or inline banner (install CLI, fix JSON, pick path).

    Disable Dispatch when no valid plan. Disable sandbox when engine unavailable.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Failure paths are understandable</done>
</task>

</tasks>

<verification>
- [ ] Empty app is guided
- [ ] CLI-missing errors clear
</verification>

<success_criteria>
- UX polish done
</success_criteria>

<output>
Create `.planning/phases/06-overview-polish/06-02-SUMMARY.md`
</output>
