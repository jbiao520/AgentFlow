---
phase: 02-persistence
plan: 03
type: execute
---

<objective>
Implement Goals / Plans / TaskRuns / TaskNodes / TaskLogs repositories and IPC read/write APIs without executing CLIs.

Purpose: Orchestrator (Phase 5) needs durable run graphs before Runner exists.
Output: Task domain persistence + TS API; Phase 2 complete.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/02-persistence/02-02-SUMMARY.md
@src-tauri/src/repo/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Task-domain repositories</name>
  <files>src-tauri/src/repo/tasks.rs, src-tauri/src/repo/mod.rs</files>
  <action>
    Implement:

    - `create_goal(prompt, template_key?)`
    - `save_plan(goal_id, analysis_json)`
    - `create_task_run(goal_id, plan_id)` + `insert_task_nodes(run_id, nodes[])`
    - `list_task_runs(limit)`, `get_task_run(id)` with nodes
    - `update_node_status`, `append_task_log`, `list_task_logs(run_id, agent_filter?)`
    - `update_run_progress`

    Status enums as TEXT: pending|running|success|failed|skipped for nodes; queued|running|success|failed|cancelled for runs.

    JSON columns store arrays/objects as TEXT. Keep queries simple.
  </action>
  <verify>`cargo test` creates goal→plan→run→nodes→log and lists them</verify>
  <done>Task graph persists in SQLite</done>
</task>

<task type="auto">
  <name>Task 2: IPC + frontend task API stubs</name>
  <files>src-tauri/src/commands/tasks.rs, src/lib/api/tasks.ts, src-tauri/src/lib.rs</files>
  <action>
    Expose commands mirroring repo functions needed by UI later. Document that execution is Phase 5.

    No DAG runner. No CLI spawn.
  </action>
  <verify>`cargo check` + `npm run build` pass</verify>
  <done>Task IPC available; Phase 2 persistence stack complete</done>
</task>

</tasks>

<verification>
- [ ] Task domain tests pass
- [ ] ROADMAP Phase 2 → Complete when SUMMARY written
</verification>

<success_criteria>
- Phase 2 done; ready for Agent & Skill UI (Phase 3)
</success_criteria>

<output>
Create `.planning/phases/02-persistence/02-03-SUMMARY.md` — Phase 2 complete.
</output>
