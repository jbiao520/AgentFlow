---
phase: 02-persistence
plan: 01
type: execute
---

<objective>
Add SQLite persistence foundation: app data directory, schema migration, and a shared Db handle used by later repositories.

Purpose: All Agent/Skill/Task state must survive app restarts (SPEC §6).
Output: Migrated empty DB under Application Support; Rust `db` module ready for repos.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/SPEC.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@src-tauri/Cargo.toml
@src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rusqlite + app data path helpers</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/db/mod.rs, src-tauri/src/db/path.rs, src-tauri/src/lib.rs</files>
  <action>
    Add `rusqlite` with `bundled` feature (avoid system SQLite linking pain on macOS). Optionally `directories` crate for app data path.

    Resolve DB path: `~/Library/Application Support/AgentFlow/agentflow.db` (create parent dirs). Provide `db_path() -> PathBuf` and `open_db() -> Result&lt;Connection&gt;`.

    Wire module in `lib.rs`. Do not create domain repos yet. Do not change UI.
  </action>
  <verify>`cargo check --manifest-path src-tauri/Cargo.toml` exits 0</verify>
  <done>DB path helper + Connection open compile; Application Support dir creatable</done>
</task>

<task type="auto">
  <name>Task 2: Implement schema v1 migration matching SPEC</name>
  <files>src-tauri/src/db/migrate.rs, src-tauri/src/db/schema.sql</files>
  <action>
    Create tables exactly as SPEC §6: agents, agent_model_profiles, skills, orchestrator_settings, goals, plans, task_runs, task_nodes, task_logs, cli_engine_status.

    Use `schema_migrations(version INTEGER PRIMARY KEY)` or `PRAGMA user_version`. Idempotent `migrate(conn)`.

    Seed `orchestrator_settings` row id=1 with defaults (cli_engine=`codex`, model=`sol`, reasoning_effort=`medium`) if missing.

    Use TEXT ids (UUID strings) or INTEGER AUTOINCREMENT consistently — prefer TEXT UUID for agents/runs for frontend ease.

    Avoid ORM. Avoid cloud sync.
  </action>
  <verify>Unit-style Rust test or a `#[cfg(test)]` that opens tempfile DB, migrates, asserts table count via `sqlite_master`; `cargo test --manifest-path src-tauri/Cargo.toml db::` passes</verify>
  <done>Fresh DB migrates; orchestrator_settings seeded; tests green</done>
</task>

<task type="auto">
  <name>Task 3: Manage Db state in Tauri app lifecycle</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/state.rs</files>
  <action>
    On startup: open DB, migrate, store `Mutex&lt;Connection&gt;` (or `Arc&lt;Mutex&lt;Connection&gt;&gt;`) in Tauri managed state.

    Add command `db_health() -> Result&lt;{ path: String, ok: bool }, String&gt;` for smoke.

    Expose on frontend later in 02-02; for now only Rust + register command. Update capabilities allowlist.
  </action>
  <verify>`cargo check` passes; `db_health` registered in invoke_handler</verify>
  <done>App boots with migrated DB in managed state</done>
</task>

</tasks>

<verification>
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` migration tests pass
- [ ] `cargo check` passes
- [ ] Schema matches SPEC table list
</verification>

<success_criteria>
- SQLite foundation ready for repository layer
- Next: 02-02 Agent/Skill/Settings repos + IPC
</success_criteria>

<output>
Create `.planning/phases/02-persistence/02-01-SUMMARY.md` then proceed to 02-02.
</output>
