---
phase: 02-persistence
plan: 02
type: execute
---

<objective>
Implement Agent, Skill, Model Profile, and Orchestrator Settings repositories with Tauri IPC and a typed frontend API — no UI binding yet beyond optional smoke.

Purpose: Phase 3 will render real lists; this plan provides the CRUD backend contract.
Output: Working IPC CRUD for registry entities; data survives restart.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/02-persistence/02-01-SUMMARY.md
@src-tauri/src/db/mod.rs
@src/lib/tauri.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust repositories for agents, profiles, skills, orchestrator</name>
  <files>src-tauri/src/repo/agents.rs, src-tauri/src/repo/skills.rs, src-tauri/src/repo/orchestrator.rs, src-tauri/src/repo/mod.rs</files>
  <action>
    Implement serde types + functions:

    - agents: list, get, upsert, delete (hard delete cascades skills/profiles via FK ON DELETE CASCADE)
    - agent_model_profiles: get_by_agent, upsert
    - skills: list_by_agent, upsert_many (for scanner), set_enabled(id, bool)
    - orchestrator_settings: get, update

    Validate: agent `name` unique; `workspace_path` non-empty on upsert.
    Return clear `Err(String)` for IPC.
  </action>
  <verify>`cargo test` covering insert agent + list; profile upsert; skill enable toggle</verify>
  <done>Repos CRUD compiles and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Register Tauri commands + frontend API module</name>
  <files>src-tauri/src/commands/agents.rs, src-tauri/src/commands/settings.rs, src/lib/api/agents.ts, src/lib/api/settings.ts, src/lib/tauri.ts, src-tauri/capabilities/default.json</files>
  <action>
    Commands (snake_case): `list_agents`, `upsert_agent`, `delete_agent`, `get_agent_profile`, `upsert_agent_profile`, `list_skills`, `set_skill_enabled`, `get_orchestrator_settings`, `update_orchestrator_settings`.

    Frontend wrappers in `src/lib/api/*` using invoke; browser mock fallbacks return empty arrays / defaults when not in Tauri.

    Extend `src/lib/tauri.ts` convention comment. No UI grid wiring yet (Phase 3).
  </action>
  <verify>`npm run build` + `cargo check` pass; commands listed in generate_handler</verify>
  <done>IPC contract usable from TS</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Agent/Skill/Settings repos + IPC (no full UI yet).</what-built>
  <how-to-verify>
    1. `npm run tauri dev`
    2. In webview console: upsert a fake agent via `__agentmindDebug` or temporary expose, then `list_agents` returns it
    3. Quit and relaunch — agent still listed
    4. Update orchestrator settings and confirm get returns new values after restart
  </how-to-verify>
  <resume-signal>Type "approved" or describe persistence issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] CRUD IPC works across restart
- [ ] Human checkpoint approved
</verification>

<success_criteria>
- Registry persistence ready for Phase 3 UI
</success_criteria>

<output>
Create `.planning/phases/02-persistence/02-02-SUMMARY.md`
</output>
