---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Initialize Tauri 2 + Vite + TypeScript (vanilla-ts) in the AgentFlow repo and migrate the HTML prototype shell so five views render inside the desktop window.

Purpose: Establish the runnable macOS app skeleton every later phase builds on. Prefer vanilla-ts over React/Vue so we can port `ai-agent-platform.html` with minimal framework rewrite in Phase 1.
Output: Working `npm run tauri dev` app showing the migrated UI shell; prototype file kept as visual reference.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/SPEC.md
@ai-agent-platform.html
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 + Vite vanilla-ts in repo root</name>
  <files>package.json, index.html, vite.config.ts, tsconfig.json, tsconfig.node.json, src/main.ts, src/styles.css, src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/src/lib.rs, src-tauri/src/main.rs, src-tauri/capabilities/default.json, README.md</files>
  <action>
    Scaffold AgentFlow as a Tauri 2 app in the existing repo root (already has `.planning/` and `ai-agent-platform.html`).

    Use non-interactive create-tauri-app when possible, e.g.:
    `npm create tauri-app@latest . -- --template vanilla-ts --manager npm --yes`
    If the scaffolder refuses a non-empty directory, scaffold into a temp dir and move frontend/`src-tauri` files into the repo without deleting `.planning/` or `ai-agent-platform.html`.

    Requirements:
    - App / product name: AgentFlow; window title: AgentFlow
    - Tauri 2.x + `@tauri-apps/api` / `@tauri-apps/cli` 2.x
    - Package manager: npm (lock with package-lock.json)
    - Configure `vite.config.ts` so Vite ignores `src-tauri/**`
    - Update `.gitignore` if scaffolder adds useful ignores; keep existing entries
    - Set macOS-friendly window defaults in `tauri.conf.json` (reasonable min size ~1100x700, title AgentFlow)
    - Do NOT add React/Vue/Svelte. Do NOT implement SQLite, CLI adapters, or real Agent logic yet.
  </action>
  <verify>`test -f package.json &amp;&amp; test -f src-tauri/tauri.conf.json &amp;&amp; npm install &amp;&amp; npx tauri --version` shows 2.x; `npm run build` (Vite) succeeds</verify>
  <done>Tauri 2 + vanilla-ts project exists in repo root; Vite build passes; `.planning/` and prototype HTML preserved</done>
</task>

<task type="auto">
  <name>Task 2: Migrate prototype CSS + five view panes into Vite app</name>
  <files>src/styles.css, src/main.ts, index.html, src/ui/shell.html (or equivalent partials inlined from main.ts)</files>
  <action>
    Port `ai-agent-platform.html` into the Vite app so the desktop UI matches the prototype shell:

    1. Move `:root` tokens and all CSS into `src/styles.css` (or split `src/styles/tokens.css` + `src/styles/app.css` if clearer — keep under 2 CSS entry files).
    2. Place the app markup (titlebar, sidebar, five `#view-*` panes, Cmd+K / import / skill modals, toast container) into the Vite-served DOM. Prefer: `index.html` mounts `#app`, and `src/main.ts` injects HTML from a typed string module or `src/ui/app-shell.ts` exporting the markup — avoid leaving a second competing root layout.
    3. Keep view ids stable for later plans: `view-overview`, `view-agents`, `view-agent-detail`, `view-commander`, `view-tasks`. Keep `data-od-id` attributes where present.
    4. Temporarily keep prototype interaction as inline-equivalent functions in `src/main.ts` OR a single `src/ui/prototype-actions.ts` so clicks do not throw (switchView stubs OK). Full routing cleanup is Plan 01-02 — do not over-refactor.
    5. Remove fake traffic-light drag region conflicts if they break Tauri (optional decorative dots OK). Do not implement custom titlebar traffic-light behavior.
    6. Keep `ai-agent-platform.html` at repo root as the visual source of truth; do not delete it.

    Avoid rewriting content into React components. Avoid changing copy/layout tokens arbitrarily.
  </action>
  <verify>`npm run build` succeeds; open `dist` or `npm run dev` and confirm `#view-overview` exists in DOM; grep shows five `view-` ids in src</verify>
  <done>Five prototype panes render under Vite; styles match prototype tokens; build clean</done>
</task>

<task type="auto">
  <name>Task 3: Verify tauri dev / build toolchain</name>
  <files>README.md, package.json</files>
  <action>
    Ensure scripts in `package.json` include at least: `dev` (vite), `build` (tsc + vite build), `tauri` / `tauri dev` / documented `npm run tauri dev`.

    Run `npm run tauri build` OR at minimum compile-check Rust with `cargo check --manifest-path src-tauri/Cargo.toml` if full DMG/app bundling is too slow — prefer a successful `cargo check` + Vite build as the gate if `tauri build` exceeds ~10 minutes.

    Update root `README.md` with: prerequisites (Rust, Xcode CLT), `npm install`, `npm run tauri dev`, pointer to `.planning/SPEC.md`.

    Commit this plan's work when done (foundation scaffold + migration) with a clear message; do not commit secrets.
  </action>
  <verify>`cargo check --manifest-path src-tauri/Cargo.toml` exits 0; `npm run build` exits 0; README documents `tauri dev`</verify>
  <done>Toolchain verified; README usable; changes committed</done>
</task>

</tasks>

<verification>
- [ ] `npx tauri --version` is 2.x
- [ ] `npm run build` passes
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml` passes
- [ ] DOM contains all five view panes
- [ ] `.planning/` and `ai-agent-platform.html` still present
</verification>

<success_criteria>
- Tauri 2 vanilla-ts app boots from this repo
- Prototype shell visually present in the webview build
- Ready for 01-02 routing cleanup without re-scaffolding
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

# Phase 1 Plan 01: Scaffold + Prototype Migration Summary

**[One-liner of what shipped]**

## Accomplishments
- ...

## Files Created/Modified
- ...

## Decisions Made
- ...

## Issues Encountered
- ...

## Next Step
Ready for `01-02-PLAN.md`
</output>
