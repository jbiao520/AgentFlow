---
phase: 01-foundation
plan: 03
type: execute
---

<objective>
Establish the Tauri command / frontend API convention with a working hello-path: ping, app info, and reveal-path — proving IPC before Phase 2 persistence.

Purpose: Freeze how UI talks to Rust so later Agent/CLI features plug into one pattern.
Output: Rust commands + `src/lib/tauri.ts` API module + visible app info in UI; Phase 1 complete after human verify.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src-tauri/src/lib.rs
@src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rust commands ping, app_info, reveal_in_finder</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/commands/mod.rs, src-tauri/src/commands/system.rs, src-tauri/capabilities/default.json, src-tauri/Cargo.toml</files>
  <action>
    Implement three Tauri 2 commands (names exact for frontend binding):

    1. `ping() -> String` returns `"pong"`
    2. `app_info() -> AppInfo` where `AppInfo { name: String, version: String, tauri_version: String }` — name/version from `tauri.conf.json` / env (`CARGO_PKG_VERSION` OK for version)
    3. `reveal_in_finder(path: String) -> Result<(), String>` — on macOS use `open -R &lt;path&gt;` via `std::process::Command`; validate path exists; return Err with message if missing. (Linux/Windows stubs may return Err("unsupported on this OS") for now — v1 is macOS-only.)

    Register commands in `invoke_handler`. Keep modules thin: `commands/system.rs`.

    Capabilities: allow these commands in `capabilities/default.json` per Tauri 2 ACL (`core:default` plus explicit allowlist if required by generated scaffolding).

    Do NOT add SQLite crates yet. Do NOT spawn agent CLIs.
  </action>
  <verify>`cargo check --manifest-path src-tauri/Cargo.toml` exits 0; commands appear in `invoke_handler!`</verify>
  <done>Three commands compile and are registered</done>
</task>

<task type="auto">
  <name>Task 2: Frontend API layer + surface app info in shell</name>
  <files>src/lib/tauri.ts, src/ui/app-info.ts, src/main.ts, package.json</files>
  <action>
    Create `src/lib/tauri.ts` wrapping `@tauri-apps/api/core` `invoke`:

    ```ts
    export async function ping(): Promise&lt;string&gt;
    export async function getAppInfo(): Promise&lt;{ name: string; version: string; tauri_version: string }&gt;
    export async function revealInFinder(path: string): Promise&lt;void&gt;
    ```

    Guard browser-only `npm run dev`: if `import.meta.env` / `window.__TAURI_INTERNALS__` absent, return mock values and no-op reveal — so Vite-only preview does not crash.

    UI wiring (minimal):
    - On shell load in Tauri, call `getAppInfo` and show `name@version` in the titlebar badge or sidebar footer (prototype has titlebar badge — reuse it)
    - Optional: small «IPC» status text that shows `ping → pong` once on load (muted), proving round-trip
    - Wire one safe demo for reveal: e.g. button only in Agent 详情 that reveals a well-known path later; for now a hidden/dev click on the version badge can `revealInFinder` on the app resource dir OR skip reveal UI if awkward — must still unit-call reveal in a tiny `src/lib/tauri.test` is NOT required; instead add `window.__agentmindDebug = { ping, getAppInfo, revealInFinder }` for console testing in tauri dev

    Keep API surface documented in a short comment at top of `tauri.ts` (this is the convention for Phase 2+).
  </action>
  <verify>`npm run build` passes; `src/lib/tauri.ts` exports the three functions; titlebar/badge updates when running under Tauri (code path present)</verify>
  <done>Frontend can invoke all three commands; app version visible in UI under Tauri</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>IPC hello-path: ping, app_info, reveal_in_finder + frontend API module.</what-built>
  <how-to-verify>
    1. Run: `npm run tauri dev`
    2. Confirm titlebar/sidebar shows AgentMind version from `app_info` (not blank)
    3. Open DevTools console in the webview if available, or use `__agentmindDebug`: run `await __agentmindDebug.ping()` → `"pong"`
    4. Run `await __agentmindDebug.getAppInfo()` → object with name/version
    5. Optional: `await __agentmindDebug.revealInFinder('/Applications')` (or another existing path) → Finder reveals; invalid path returns a clear error
    6. Confirm five-view navigation from 01-02 still works
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe IPC issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml` passes
- [ ] `npm run build` passes
- [ ] ping / app_info work in `tauri dev`
- [ ] Human checkpoint approved
- [ ] Update `.planning/ROADMAP.md` Phase 1 status → Complete when SUMMARY written
</verification>

<success_criteria>
- IPC convention documented in `src/lib/tauri.ts`
- Hello-path proven end-to-end in Tauri window
- Phase 1 Foundation complete; next is Phase 2 Persistence
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 03: IPC Skeleton Summary

**[One-liner]**

## Accomplishments
- ...

## Files Created/Modified
- ...

## Decisions Made
- ...

## Issues Encountered
- ...

## Next Step
Phase 1 complete — ready to plan or execute Phase 2 Persistence (`02-persistence`)
</output>
