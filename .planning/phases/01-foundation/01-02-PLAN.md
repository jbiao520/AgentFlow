---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Modularize view routing and shell interactions so sidebar navigation, five views, and modal overlays work reliably without depending on fragile inline prototype globals.

Purpose: Make the UI shell a stable navigation surface before wiring real IPC/data.
Output: Typed view router + modal controllers; Cmd+K / Import / Skill modals open/close; human-verified five-page navigation.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@ai-agent-platform.html
@src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typed view router and sidebar active state</name>
  <files>src/ui/router.ts, src/main.ts, src/ui/nav.ts</files>
  <action>
    Replace ad-hoc `switchView` / scattered `onclick` string coupling with a small typed module:

    - Define `export type ViewId = 'overview' | 'agents' | 'agent-detail' | 'commander' | 'tasks'`
    - `showView(id: ViewId)` toggles `.view-pane.active` on `#view-${id}` (or matching ids from 01-01), updates sidebar `.nav-item.active`, and scrolls content to top
    - Centralize nav click binding in `src/ui/nav.ts` (query `[data-od-id=nav-*]` or `data-view` attributes). Prefer adding `data-view="overview"` etc. on nav items for clarity — keep labels/icons from prototype
    - `selectAgent(name: string)` may still only update detail header text + call `showView('agent-detail')` with mock strings — no SQLite yet
    - Expose `showView` for other modules; avoid putting business logic here
    - Do not introduce a SPA framework. Do not fetch real agents.
  </action>
  <verify>`npm run build` passes; unit-free manual script optional — grep confirms `ViewId` and `showView` exist; no remaining broken references to undefined `switchView` unless re-exported as alias</verify>
  <done>All five nav items call the typed router; only one view pane active at a time</done>
</task>

<task type="auto">
  <name>Task 2: Wire Cmd+K, Import, and Skill modal controllers</name>
  <files>src/ui/modals.ts, src/main.ts, src/ui/toast.ts</files>
  <action>
    Extract modal open/close into `src/ui/modals.ts`:

    - Cmd+K: open on button + `Meta+K` / `Ctrl+K`; close on ESC / overlay click; filter list client-side (prototype behavior OK)
    - Import Agent modal: open from Agent 矩阵 primary button; cancel/overlay close; submit may `toast` mock success (no persistence)
    - Skill detail modal: open from skill row click with filename + description args; close button/overlay
    - Toast helper in `src/ui/toast.ts` (port prototype toast)

    Bind events from `main.ts` after DOM ready. Keep commander/task mock buttons callable without throwing (Orchestrate may still toast or toggle `#orch-results` visibility as in prototype).

    Avoid implementing real import/scan. Avoid new design systems.
  </action>
  <verify>`npm run build` passes; `src/ui/modals.ts` exports open/close for the three modals; keyboard listener registered in main</verify>
  <done>Modals open/close without errors; toast works; mock actions do not throw</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Typed router + modal shell on top of the migrated prototype UI (after 01-01 scaffold).</what-built>
  <how-to-verify>
    1. Run: `npm run tauri dev` (or `npm run dev` in browser if webview blocked — prefer Tauri window)
    2. Click each sidebar item: 全局总览 / Agent 矩阵 / Agent 详情 / 调度中枢 / 任务中心 — confirm correct pane, active nav highlight
    3. From Agent 矩阵 click a card — lands on Agent 详情 with name updated
    4. Open Cmd+K (`⌘K`), navigate via a quick action, ESC closes
    5. Open「导入/新建 Agent 工作区」modal and dismiss; open a Skill preview from Agent 详情 and dismiss
    6. Confirm no blank panes, no JS console errors on these paths
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe UI/nav issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] Five views switch with single active pane
- [ ] Three modals open/close
- [ ] Human checkpoint approved
</verification>

<success_criteria>
- Navigation shell is typed and stable
- Prototype interaction surface usable for demos without real backend
- Ready for IPC skeleton in 01-03
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 02: Router + Shell Interactions Summary

**[One-liner]**

## Accomplishments
- ...

## Files Created/Modified
- ...

## Decisions Made
- ...

## Issues Encountered
- ...

## Next Step
Ready for `01-03-PLAN.md`
</output>
