---
phase: 04-cli-runtime
plan: 01
type: execute
---

<objective>
Detect local availability of cursor-agent, codex, and opencode CLIs; surface status in the sidebar widget and cache in SQLite.

Purpose: UI and Runner must know which engines can run (SPEC §7.2 CliProbe).
Output: Probe command + live CLI Engines widget.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/03-agent-skill/03-03-SUMMARY.md
@src/ui/app-shell.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: CliProbe Rust service</name>
  <files>src-tauri/src/services/cli_probe.rs, src-tauri/src/repo/cli_status.rs, src-tauri/src/commands/cli.rs</files>
  <action>
    For each engine name (`cursor-agent`, `codex`, `opencode`): resolve binary via `which` / common install paths; run `--version` (or engine-specific version flag) with short timeout; store available/version/last_checked_at in `cli_engine_status`.

    Commands: `probe_cli_engines() -> Vec&lt;EngineStatus&gt;`, `list_cli_engine_status()`.

    Never fail app startup if probe fails — mark unavailable.
  </action>
  <verify>`cargo check` passes; probe returns three entries</verify>
  <done>Probe persists and returns statuses</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar CLI widget binding</name>
  <files>src/ui/cli-widget.ts, src/main.ts, src/lib/api/cli.ts</files>
  <action>
    On app load + manual refresh: render dots/labels from probe results. Show `n/3 Active`. Replace hardcoded "3/3 Active" prototype text.
  </action>
  <verify>`npm run build` passes</verify>
  <done>Sidebar reflects real CLI presence</done>
</task>

</tasks>

<verification>
- [ ] Widget matches installed tools on machine
</verification>

<success_criteria>
- CliProbe ready for adapters
</success_criteria>

<output>
Create `.planning/phases/04-cli-runtime/04-01-SUMMARY.md`
</output>
