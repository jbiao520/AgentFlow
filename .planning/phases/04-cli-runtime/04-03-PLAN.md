---
phase: 04-cli-runtime
plan: 03
type: execute
---

<objective>
Connect Agent detail sandbox to real CLI execution with streaming logs in the terminal panel via Tauri events.

Purpose: First end-to-end proof that AgentMind can drive a workspace CLI (SPEC §5.3 sandbox).
Output: Sandbox run button executes; Phase 4 complete after verify.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/04-cli-runtime/04-02-SUMMARY.md
@.planning/phases/04-cli-runtime/CLI_FLAGS.md
@src/ui/agents/detail-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sandbox_run command + event channel</name>
  <files>src-tauri/src/commands/sandbox.rs, src-tauri/src/lib.rs, src/lib/api/sandbox.ts</files>
  <action>
    `sandbox_run { agent_id, prompt }` loads agent+profile, validates engine available, spawns adapter, emits `sandbox-log` events to frontend, returns `{ exit_code }`.

    Support cancel via `sandbox_cancel` if feasible; otherwise document as follow-up.

    Append nothing to task_runs (sandbox is ephemeral) unless easy — prefer ephemeral only.
  </action>
  <verify>`cargo check` passes</verify>
  <done>IPC sandbox run streams events</done>
</task>

<task type="auto">
  <name>Task 2: Wire sandbox terminal UI</name>
  <files>src/ui/agents/sandbox.ts, src/main.ts</files>
  <action>
    Replace mock `runSandboxTest` timer with real invoke + listen(`sandbox-log`). Append lines to `#sandbox-term`. Disable button while running. Toast on completion/failure.
  </action>
  <verify>`npm run build` passes</verify>
  <done>UI streams real CLI output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real CLI sandbox for one Agent.</what-built>
  <how-to-verify>
    1. Ensure at least one of cursor-agent/codex/opencode is installed
    2. Import workspace, set matching CLI, run sandbox with a trivial prompt
    3. Confirm terminal shows live lines and exit completes
  </how-to-verify>
  <resume-signal>Type "approved" — Phase 4 complete (or note which engines missing)</resume-signal>
</task>

</tasks>

<verification>
- [ ] At least one engine sandboxed successfully (or documented skip)
- [ ] Human approved
</verification>

<success_criteria>
- Phase 4 CLI Runtime complete
</success_criteria>

<output>
Create `.planning/phases/04-cli-runtime/04-03-SUMMARY.md` — Phase 4 complete.
</output>
