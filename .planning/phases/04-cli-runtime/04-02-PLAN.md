---
phase: 04-cli-runtime
plan: 02
type: execute
---

<objective>
Define EngineAdapter trait/API and implement minimal runnable wrappers for cursor-agent, codex, and opencode that spawn with cwd + prompt (+ optional model/reasoning flags).

Purpose: Isolate CLI flag differences from UI/Runner (SPEC §7.3).
Output: Unified `run_engine` capable of streaming stdout/stderr lines.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/04-cli-runtime/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research local CLI flags and document findings</name>
  <files>.planning/phases/04-cli-runtime/CLI_FLAGS.md, src-tauri/src/engines/mod.rs</files>
  <action>
    On the build machine, run `cursor-agent --help`, `codex --help`, `opencode --help` (skip gracefully if missing). Write `CLI_FLAGS.md` with the exact argv patterns chosen for: prompt, model, reasoning/effort, working directory.

    Prefer non-interactive flags. Avoid inventing flags — if unknown, use stdin prompt or positional args proven by help text.
  </action>
  <verify>CLI_FLAGS.md exists with three sections</verify>
  <done>Flag choices documented from real CLIs</done>
</task>

<task type="auto">
  <name>Task 2: Implement adapters + process spawn with line streaming</name>
  <files>src-tauri/src/engines/adapter.rs, src-tauri/src/engines/cursor_agent.rs, src-tauri/src/engines/codex.rs, src-tauri/src/engines/opencode.rs, src-tauri/src/engines/runner.rs</files>
  <action>
    Shared input: `{ engine, cwd, prompt, model?, reasoning?, extra_args?, env? }`.

    Spawn child process; kill on drop/cancel token; emit log events `{ ts, stream: stdout|stderr, line }`; return exit code.

    Use tokio or std threads as fits current Tauri runtime — keep compile-clean.

    Do not implement DAG yet. Security: cwd must be an imported agent workspace path (validate against DB).
  </action>
  <verify>`cargo check` passes; optional ignored integration test behind `#[ignore]`</verify>
  <done>Three adapters + shared runner compile</done>
</task>

</tasks>

<verification>
- [ ] CLI_FLAGS.md accurate
- [ ] Adapters compile
</verification>

<success_criteria>
- Ready to wire sandbox UI streaming
</success_criteria>

<output>
Create `.planning/phases/04-cli-runtime/04-02-SUMMARY.md`
</output>
